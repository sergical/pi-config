# Sensitive - don't sync
auth.json

# Local state - don't sync
.pi/
sessions/
bin/
settings.json
models.json

# Local state
sentry-monitor.json

# Symlinked skills (installed locally, not portable)
skills/qmd-knowledge

# Pi runtime files (created when this repo is ~/.pi/agent/)
git/
mcp-cache.json
mcp-npx-cache.json
run-history.jsonl
session-manager-config.toml

# Extension dependencies (install locally)
extensions/*/node_modules/

# OS files
.DS_Store
Thumbs.db
tmp/
